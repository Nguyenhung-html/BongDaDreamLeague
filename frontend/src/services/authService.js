// src/services/authService.js
export default {
  login(payload) {
    // TODO: gọi API đăng nhập thật
    return Promise.resolve({ user: null })
  },
  register(payload) {
    // TODO: gọi API đăng ký thật
    return Promise.resolve({ user: null })
  },
  logout() {
    // TODO: xoá token / phiên đăng nhập
  }
}